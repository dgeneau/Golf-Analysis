<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Merged with the Xcode-generated Info.plist (GENERATE_INFOPLIST_FILE stays on).
	     bluetooth-central: the DOT keeps streaming while the phone is locked.
	     location: the GPS trail keeps recording so locked-phone swings are
	     placed where they were hit, not where the phone woke up. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>bluetooth-central</string>
		<string>location</string>
	</array>
</dict>
</plist>
